# Reference copy of the sshd drop-in that 10-base.sh installs to
# /etc/ssh/sshd_config.d/10-rvc-hardening.conf on the VM.
# Defense in depth — both transports are already private, but key-only auth is cheap.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
